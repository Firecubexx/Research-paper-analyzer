*, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

:root {
  --bg: #08080f;
  --bg2: #0f0f1a;
  --bg3: #161624;
  --bg4: #1e1e2e;
  --bg5: #252538;
  --accent: #7c6fff;
  --accent2: #a78bfa;
  --accent3: #38bdf8;
  --accent4: #34d399;
  --danger: #f87171;
  --warn: #fbbf24;
  --border: rgba(255,255,255,0.07);
  --border2: rgba(255,255,255,0.14);
  --text: #eeeeff;
  --text2: #9090b0;
  --text3: #505070;
  --radius: 14px;
  --radius-sm: 8px;
}

html { scroll-behavior: smooth; }
body {
  background: var(--bg);
  color: var(--text);
  font-family: 'DM Sans', sans-serif;
  min-height: 100vh;
  font-size: 15px;
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
}

h1,h2,h3,.brand { font-family: 'Syne', sans-serif; }

::-webkit-scrollbar { width: 4px; }
::-webkit-scrollbar-track { background: var(--bg2); }
::-webkit-scrollbar-thumb { background: var(--bg5); border-radius: 2px; }

a { color: var(--accent3); }

/* ── Topbar ── */
.topbar {
  display: flex; align-items: center; justify-content: space-between;
  padding: 0 28px; height: 58px;
  background: var(--bg2); border-bottom: 1px solid var(--border);
  position: sticky; top: 0; z-index: 100;
}
.brand {
  font-family: 'Syne', sans-serif; font-size: 17px; font-weight: 700;
  letter-spacing: -0.5px; cursor: pointer; user-select: none;
  background: linear-gradient(135deg, var(--accent2), var(--accent3));
  -webkit-background-clip: text; -webkit-text-fill-color: transparent;
}
.brand span { opacity: 0.5; font-weight: 400; }
.nav { display: flex; gap: 3px; }
.nav-btn {
  display: flex; align-items: center; gap: 6px;
  padding: 7px 15px; border-radius: 8px; border: none;
  background: transparent; color: var(--text2);
  font-family: 'DM Sans', sans-serif; font-size: 13px; font-weight: 500;
  cursor: pointer; transition: all 0.15s;
}
.nav-btn:hover { background: var(--bg3); color: var(--text); }
.nav-btn.active { background: var(--bg4); color: var(--accent2); }
.topbar-right { display: flex; align-items: center; gap: 10px; }
.api-status {
  display: flex; align-items: center; gap: 6px;
  font-size: 12px; color: var(--text3);
}
.dot { width: 7px; height: 7px; border-radius: 50%; flex-shrink: 0; }
.dot-red { background: var(--danger); }
.dot-green { background: var(--accent4); }
.upload-count {
  font-size: 11px; padding: 4px 10px; border-radius: 20px;
  background: rgba(124,111,255,0.1); color: var(--accent2);
  border: 1px solid rgba(124,111,255,0.2);
}

/* ── Main ── */
.main {
  max-width: 1160px; margin: 0 auto;
  padding: 32px 28px; min-height: calc(100vh - 58px - 48px);
}
.page { display: none; animation: fadeUp 0.28s ease; }
.page.active { display: block; }

@keyframes fadeUp {
  from { opacity: 0; transform: translateY(10px); }
  to   { opacity: 1; transform: translateY(0); }
}
@keyframes bounce {
  0%,80%,100% { transform: translateY(0); }
  40% { transform: translateY(-7px); }
}
@keyframes progressAnim {
  0% { left: -40%; width: 40%; }
  100% { left: 100%; width: 40%; }
}

.page-title { font-size: 24px; font-weight: 700; letter-spacing: -0.5px; margin-bottom: 5px; }
.page-sub { font-size: 14px; color: var(--text2); margin-bottom: 26px; }

/* ── Cards ── */
.card {
  background: var(--bg2); border: 1px solid var(--border);
  border-radius: var(--radius); padding: 20px;
}
.section-label {
  font-size: 11px; font-weight: 600; letter-spacing: 1px;
  text-transform: uppercase; color: var(--text3); margin-bottom: 10px;
}

/* ── Buttons ── */
.btn {
  display: inline-flex; align-items: center; gap: 7px;
  border-radius: 8px; border: none; cursor: pointer;
  font-family: 'DM Sans', sans-serif; font-weight: 500;
  transition: all 0.18s; white-space: nowrap;
  padding: 9px 18px; font-size: 14px;
}
.btn:disabled { opacity: 0.4; cursor: not-allowed; }
.btn:hover:not(:disabled) { opacity: 0.85; transform: translateY(-1px); }
.btn-primary { background: var(--accent); color: #fff; }
.btn-ghost {
  background: transparent; color: var(--text2);
  border: 1px solid var(--border2);
}
.btn-ghost:hover:not(:disabled) { background: var(--bg3); color: var(--text); }
.btn-danger { background: rgba(248,113,113,0.12); color: var(--danger); border: 1px solid rgba(248,113,113,0.25); }
.btn-sm { padding: 6px 13px; font-size: 13px; }
.btn-full { width: 100%; justify-content: center; padding: 13px; }

/* ── Inputs ── */
input, textarea, select {
  font-family: 'DM Sans', sans-serif; font-size: 14px;
  color: var(--text); background: var(--bg4);
  border: 1px solid var(--border2); border-radius: 8px;
  outline: none; transition: border-color 0.2s, box-shadow 0.2s;
}
input { padding: 9px 13px; }
input:focus, textarea:focus, select:focus {
  border-color: var(--accent);
  box-shadow: 0 0 0 3px rgba(124,111,255,0.15);
}
textarea { padding: 12px; width: 100%; resize: vertical; line-height: 1.6; }
select { padding: 9px 12px; cursor: pointer; }
select option { background: var(--bg3); }

/* ── Badge ── */
.badge {
  font-size: 11px; font-weight: 500; padding: 3px 9px;
  border-radius: 20px; display: inline-flex; align-items: center; gap: 3px;
}
.badge-success { background: rgba(52,211,153,0.12); color: var(--accent4); border: 1px solid rgba(52,211,153,0.22); }
.badge-warn    { background: rgba(251,191,36,0.12);  color: var(--warn);    border: 1px solid rgba(251,191,36,0.22); }
.badge-info    { background: rgba(56,189,248,0.12);  color: var(--accent3); border: 1px solid rgba(56,189,248,0.22); }
.badge-accent  { background: rgba(124,111,255,0.12); color: var(--accent2); border: 1px solid rgba(124,111,255,0.22); }
.badge-default { background: var(--bg4); color: var(--text2); border: 1px solid var(--border2); }

/* ── Upload zone ── */
.upload-zone {
  border: 2px dashed var(--border2); border-radius: var(--radius);
  padding: 52px 24px; text-align: center; cursor: pointer;
  transition: all 0.2s; position: relative;
}
.upload-zone:hover, .upload-zone.drag {
  border-color: var(--accent); background: rgba(124,111,255,0.05);
}
.upload-icon { font-size: 42px; margin-bottom: 14px; opacity: 0.4; }
.upload-title { font-size: 17px; font-weight: 600; font-family: 'Syne',sans-serif; margin-bottom: 6px; }
.upload-sub { font-size: 13px; color: var(--text3); }

/* ── Progress ── */
.progress-track {
  height: 4px; background: var(--bg4); border-radius: 2px;
  overflow: hidden; position: relative;
}
.progress-fill {
  height: 100%; border-radius: 2px;
  background: linear-gradient(90deg, var(--accent), var(--accent3));
  transition: width 0.4s ease;
}
.progress-fill.anim {
  width: 40% !important; position: absolute;
  animation: progressAnim 1.5s infinite linear;
}
.extract-progress { margin-top: 18px; width: 100%; max-width: 280px; margin-inline: auto; }

/* ── Stats row ── */
.stats-row {
  display: grid; grid-template-columns: repeat(4, 1fr);
  gap: 12px; margin-top: 22px;
}
.stat-card {
  background: var(--bg3); border: 1px solid var(--border);
  border-radius: var(--radius); padding: 16px 18px; text-align: center;
}
.stat-num {
  font-size: 30px; font-weight: 800; font-family: 'Syne', sans-serif;
  background: linear-gradient(135deg, var(--accent2), var(--accent3));
  -webkit-background-clip: text; -webkit-text-fill-color: transparent;
}
.stat-label { font-size: 12px; color: var(--text3); margin-top: 2px; }

/* ── Papers list ── */
.papers-list { display: flex; flex-direction: column; gap: 9px; margin-top: 10px; }
.paper-item {
  background: var(--bg2); border: 1px solid var(--border);
  border-radius: 10px; padding: 13px 16px;
  display: flex; align-items: center; gap: 13px;
  transition: border-color 0.18s;
}
.paper-item:hover { border-color: var(--border2); }
.paper-icon { font-size: 22px; flex-shrink: 0; }
.paper-info { flex: 1; min-width: 0; }
.paper-name { font-size: 14px; font-weight: 500; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.paper-meta { font-size: 12px; color: var(--text3); margin-top: 2px; }
.paper-preview { font-size: 12px; color: var(--text3); margin-top: 4px; overflow: hidden; display: -webkit-box; -webkit-line-clamp: 1; -webkit-box-orient: vertical; }
.paper-badges { display: flex; flex-direction: column; gap: 5px; align-items: flex-end; flex-shrink: 0; }

/* ── Tool grid ── */
.tool-grid {
  display: grid; grid-template-columns: repeat(5, 1fr);
  gap: 10px; margin-bottom: 26px;
}
.tool-card {
  background: var(--bg3); border: 1px solid var(--border);
  border-radius: var(--radius); padding: 16px 12px;
  cursor: pointer; text-align: center; transition: all 0.18s;
}
.tool-card:hover { border-color: var(--border2); transform: translateY(-2px); }
.tool-card.active { border-color: var(--accent); background: rgba(124,111,255,0.08); }
.tool-icon { font-size: 24px; margin-bottom: 8px; }
.tool-name { font-size: 13px; font-weight: 600; margin-bottom: 4px; }
.tool-desc { font-size: 11px; color: var(--text3); line-height: 1.4; }

/* ── Analyze grid ── */
.analyze-grid {
  display: grid; grid-template-columns: 1fr 1.4fr;
  gap: 20px; align-items: start;
}
.paper-select-list {
  max-height: 280px; overflow-y: auto;
  display: flex; flex-direction: column; gap: 7px; margin-top: 8px;
}
.paper-select-item {
  display: flex; align-items: center; gap: 10px;
  padding: 9px 12px; border-radius: 8px;
  background: var(--bg3); border: 1px solid var(--border);
  cursor: pointer; transition: all 0.15s; font-size: 13px;
}
.paper-select-item:hover { border-color: var(--border2); }
.paper-select-item.selected { background: rgba(124,111,255,0.1); border-color: var(--accent); color: var(--text); }
.paper-select-name { flex: 1; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }

/* ── Error box ── */
.error-box {
  background: rgba(248,113,113,0.1); border: 1px solid rgba(248,113,113,0.25);
  border-radius: 8px; padding: 10px 14px;
  font-size: 13px; color: var(--danger); margin-top: 12px;
}

/* ── Empty state ── */
.empty-state {
  text-align: center; padding: 60px 20px; color: var(--text3);
}
.empty-icon { font-size: 44px; margin-bottom: 12px; opacity: 0.3; }

/* ── Result ── */
.result-box {
  background: var(--bg3); border: 1px solid var(--border);
  border-radius: 8px; padding: 16px; max-height: 480px; overflow-y: auto;
}
.result-box pre {
  white-space: pre-wrap; font-family: 'DM Sans', sans-serif;
  font-size: 14px; line-height: 1.75; color: var(--text);
}

/* ── Score ── */
.score-num { font-size: 30px; font-weight: 800; font-family: 'Syne'; }
.score-bar { transition: width 0.8s ease; }
.score-label { font-size: 13px; color: var(--text2); }

/* ── Refs ── */
.refs-list { max-height: 380px; overflow-y: auto; display: flex; flex-direction: column; gap: 9px; margin-top: 10px; }
.ref-item { background: var(--bg3); border: 1px solid var(--border); border-radius: 8px; padding: 12px 14px; }
.ref-num { font-size: 11px; color: var(--accent3); font-weight: 600; margin-bottom: 4px; }
.ref-text { font-size: 13px; line-height: 1.6; color: var(--text2); }

/* ── Library ── */
.library-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: 13px;
}
.lib-card {
  background: var(--bg2); border: 1px solid var(--border);
  border-radius: var(--radius); padding: 18px; transition: all 0.2s;
}
.lib-card:hover { border-color: var(--accent); transform: translateY(-2px); }
.lib-list-item {
  background: var(--bg2); border: 1px solid var(--border);
  border-radius: 10px; padding: 13px 18px;
  display: flex; align-items: center; gap: 13px;
  transition: border-color 0.18s; margin-bottom: 9px;
}
.lib-list-item:hover { border-color: var(--border2); }

/* ── Loading dots (for run button) ── */
.dots { display: inline-flex; gap: 4px; align-items: center; }
.dot-anim {
  width: 5px; height: 5px; border-radius: 50%; background: #fff;
  animation: bounce 1.2s infinite;
}
.dot-anim:nth-child(2) { animation-delay: 0.2s; }
.dot-anim:nth-child(3) { animation-delay: 0.4s; }

/* ── Footer ── */
.footer {
  display: flex; justify-content: space-between; align-items: center;
  padding: 14px 28px; border-top: 1px solid var(--border);
  font-size: 12px; color: var(--text3);
}

/* ── Responsive ── */
@media (max-width: 900px) {
  .tool-grid { grid-template-columns: repeat(3,1fr); }
  .analyze-grid { grid-template-columns: 1fr; }
  .stats-row { grid-template-columns: repeat(2,1fr); }
}
@media (max-width: 600px) {
  .topbar { padding: 0 14px; }
  .main { padding: 20px 14px; }
  .tool-grid { grid-template-columns: repeat(2,1fr); }
  .hide-mobile { display: none !important; }
  .brand { font-size: 14px; }
  .nav-btn { padding: 6px 10px; }
}
